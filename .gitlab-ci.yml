image: node:4.2.2


cache:
  paths:
  - node_modules/

services:
  - mongo
  

staging:
  image: ruby:latest
  image: octave:latest
  script:
   - gem install dpl
   - dpl --provider=heroku --app=hvu --api-key=$HEROKU_STAGING_API_KEY

